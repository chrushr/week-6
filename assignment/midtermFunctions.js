/* ================================
Week 6 Assignment: Midterm Functions + Signatures
================================ */
//**********MY FUNCTIONS*************
// Filter dataset to only save Safeway supermarkets
var Safeway = _.filter(data, function(each){
  return each.name === "Safeway"
})


// Color data points based on store names
var color = function(){
  if statements
  return
}

// change the title and description to information about that store when a data point is clicked

var eachFeature = function(layer) {

  layer.on('click', function (event) {
    if statements

    $(xxx).text(storename)
    $(xxx).text(store address/description)

    showResults();
  });
};
